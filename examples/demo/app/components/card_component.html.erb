<article class="card">
  <% if header %>
    <header class="card__header"><%= header %></header>
  <% end %>
  <h2 class="card__title"><%= title %></h2>
  <div class="card__body">
    <%= content %>
  </div>
</article>
